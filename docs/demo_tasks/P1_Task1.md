# P1_Task 1：技能装配与被动选择

仓库：[w7775p/relic-ARPG](https://github.com/w7775p/relic-ARPG)。工程入口：`game/project.godot`。技术栈：Godot 4.7.2 标准版、GDScript、3D 俯视即时动作、Windows 键鼠。

阶段：D1。状态：待验收。任务 ID：`P1_Task1`。建议分支：`feat/p1-task1-loadout`。

前置任务：M2 已合并 main。执行时以最新 `main` 为代码起点，核实前置功能已合入；本包未合并时，可从任务文档分支读取本卡，按总表说明准备功能分支。文档基线为 `313ac7f`，它只用于识别 M2 历史，禁止把它当作后续任务固定起点。

## 开始前先阅读以下文档

| 文档（仓库根目录相对路径） | 阅读目的 |
| --- | --- |
| [AGENTS.md](../../AGENTS.md) | 项目约束与「完成任务前必须检查」 |
| [README.md](../../README.md) | 当前入口、操作和已实现功能 |
| [docs/engineering.md](../../docs/engineering.md) | 当前模块、数据与存档规则 |
| [docs/tasks.md](../../docs/tasks.md) | 任务状态与前置合入情况 |
| [known_trap.md](../../known_trap.md) | 实际问题，避免重复踩坑 |
| [docs/plan.md](../../docs/plan.md) | 第 10、11 节阶段额度与验收目标 |
| [docs/demo_tasks/README.md](../../docs/demo_tasks/README.md) | 基线、执行顺序、内容归属与交接约定 |
| [docs/validation.md](../../docs/validation.md) | 现有验证入口与历史结果 |

有前置任务时同时阅读对应任务卡的验收与实际交接记录；上表文档均应存在。运行代码的最新状态优先用于判断已实现内容。

## 已经实现的功能

仓库基线已完成 M0～M2：3D 即时战斗、雷霆旋风、随机装备、拾取换装、背包仓库、出售、成长、整备及完整探险存档。用户已试玩 M2 并反馈效果可以；历史自动化记录为 92 项回归及 Windows 导出/启动通过。

SkillRunner 已运行普攻、旋风和能量；InventoryState.build() 从装备汇总 BuildDefinition。当前没有技能装配界面和独立被动选择。

上述后续阶段功能须在开工时核实。本卡的“前置完成后”描述属于依赖条件，不能直接当作当前已完成事实。

## 本次要做的任务

① 建立技能定义与装配状态：稳定技能 ID、中文名、标签、槽位适用规则、消耗和冷却说明。先接通已有普攻与旋风，保留现有攻击来源与触发行为。

② 采用三个主动槽的最小方案：左键基础攻击槽、右键主要技能槽、F 辅助槽。当前辅助槽为空；可选技能随后续任务加入。闪避、药剂单独处理，输入统一经过语义动作。

③ 制作六个真实生效的被动节点，覆盖伤害、暴击、范围、生命、护甲、回能；同时选择上限为三个。节点全部开放，在据点免费选择与重置；技能装配同样在据点调整。

④ 在现有整备界面加入装配入口、当前选择与中文属性变化。装备和被动共同重算；取消被动恢复原值，调整上限时钳制当前生命/能量，切换不能获得免费补满或重置冷却。

## 修改范围与 Godot 实现

| 主要路径 | 实现落点 |
| --- | --- |
| `game/systems/skills/、game/content/skills/（新增）` | Resource 保存技能定义，会话对象保存装配；扩展 skill_runner.gd，避免增加全局服务。 |
| `game/systems/stats/、game/content/passives/（新增）、game/systems/inventory/inventory_state.gd` | 分清装备与被动的贡献，统一产生本次战斗属性。 |
| `game/ui/inventory/、game/world/maps/expedition.gd` | 扩展现有据点入口与状态绑定；界面调用会话操作。 |

涉及路径以当前仓库检查为准；标注新增的目录由本任务按实际功能建立。共用存档入口为 `game/app/services/save_manager.gd`、`game/systems/persistence/` 与 `game/world/maps/expedition.gd`，仅在本卡确有影响时修改；相关回归放 `game/tests/` 并接入 `tools/verify.py`。节点和 API 先查项目现有用法，新增用法核对同版官方文档。新增类、函数、回调与功能写简明中文注释。

## 存档与兼容

从 main 当前版本号递增建立 D1 存档版本，提供 M2 v2 → 新版迁移。旧角色默认普攻＋旋风、辅助槽空、被动未选；物品、经验、难度、随机状态和在途战斗保持。新增装配与被动字段必须同时更新 SaveManager、SaveValidator、SessionSnapshot 和恢复入口；现有校验器会限制字段数量，禁止只向 JSON 塞字段。

## 验收标准

① 从主菜单新建角色，在据点选择三个被动并出发；用人工可算样例核对属性，取消后回到原值。第四个选择被正确拒绝，UI 显示原因。

② 旧版 M2 据点存档和战斗中存档均可继续；保存重启后装配一致，旧词条、生命、能量和冷却保持。

③ 普攻、旋风、暴击闪电和感电爆炸继续工作；新测试接入 tools/verify.py，原 M0/M1/M2 回归仍有对应覆盖。

功能验证通过 `python tools/verify.py --godot <引擎路径>` 执行；新增用例需要实际运行到完成标记。涉及显示、声音和操作的标准按可见运行与试玩记录验收，历史结果不能代替本次验证。

## 要求

1. 先检查当前项目状态：分支、工作区、最新 main、前置合入、实际代码与验证入口；保护现有未提交修改。先完成可以核实的工作，具体依赖缺失时说明缺失文件或功能。
2. 不要实现与本任务无关的系统。只参考本仓库与所需官方资料，沿用本项目约定，无需使用 `global-work-rules`。
3. 优先最小可用实现，以本卡验收为完成边界；从当前主入口可以观察到功能，必要存档与基础反馈同批完成。具体数值可调整并记录原因。
4. 完成后按 AGENTS.md「完成任务前必须检查」逐条执行，更新本卡状态、任务总表和工作日志；实际遇到新坑才更新 `known_trap.md`。提交任务分支和 PR（合并请求），合并按用户当前授权执行。
5. 最后按下表汇报，并附分支/提交、测试命令与实际结果、PR 和可用构建链接。

| 汇报项 | 必须包含 |
| --- | --- |
| 改了哪些文件 | 主要文件及用途；文档、资源、存档版本变化。 |
| 当前能做什么 | 从哪个入口操作，得到什么结果，哪些验收已经实际通过。 |
| 还不能做什么 | 未完成项、失败项、未验证项及对应影响。 |
| 下一步建议做什么 | 已解锁的下一张任务卡、待合入依赖或具体阻塞。 |

## 执行记录

基线：最新 main `313ac7fbbf0f7a087979b39543e006ce41b3c69a`；任务规范来自 `docs/demo-task-cards` 的 `5945669`。分支：`feat/p1-task1-loadout`。本分支带入配套任务包与 AGENTS 完成检查。

实现：Resource 技能与六个被动定义、会话装配、据点技能被动页、装备叠加、三项上限与错误提示。三个技能槽允许卸下；辅助槽目前无候选。六项采用固定加值：伤害 6、暴击率 0.05、旋风半径 0.5 米、生命 60、护甲 15、闲置回能 6/秒。切换保留资源与冷却，出发/撤离沿用 M2 恢复。

存档 v3：v2 校验后仅补装配字段，v1 仍走原新角色入口；覆盖快照、校验、迁移、加载、恢复。完整回归命令 `python tools/verify.py --godot <Godot 4.7.2 路径>`，Linux 123 项通过（M0 20、M1 26、M2 46、P1 31）。新增测试覆盖六属性、第四项 UI 提示、据点限制、钳制与无补满、空槽施放、v2 据点/战斗重建、随机下一步、v3 选择持久化及损坏输入。

可见验收待完成：当前 Xvfb 无法建立显示套接字，未获得本轮画面、中文排版与人工操作结果；自动回归不能替代该项。Windows 构建、提交与 PR 见下方交付记录。下一卡 P1_Task2 在本任务合入后接入横扫、战吼与药剂。


本轮交付：功能提交 `aaf8b2e7512c491601bc1426b890cd0cb7c9c6ba`，[PR #5](https://github.com/w7775p/relic-ARPG/pull/5)。对应 [Windows 工作流](https://github.com/w7775p/relic-ARPG/actions/runs/34543081309) 的回归、导出与独立启动全部通过；[下载 Windows 构建](https://github.com/w7775p/relic-ARPG/actions/runs/34543081309/artifacts/10178021093)。产物沿用工作流名称 relic-arpg-m2-windows，实际包含本卡功能。本次后续提交仅补验证交接文档，代码与已验证提交相同。可见试玩仍待验收。
