# 实际问题记录

| 编号 | 问题与原因 | 处理 | 验证 |
| --- | --- | --- | --- |
| M0-01 | 导航高度与体素精度不整除会警告；仅改变网格体素高度还会与地图默认值不匹配 | 保留默认体素，导航净空高度设为 2 米，为 1.8 米角色留余量 | 重新烘焙并验证绕障路径 |
| M0-02 | 初始存档坐标限制小于场地可站立范围，贴墙时保存会被拒绝 | 按墙体内侧与胶囊半径允许至 19.2 米 | 回归包含贴墙保存 |
| M0-03 | 手写输入配置误用了左右键码；直接调用动作的测试无法发现物理键位错误 | 使用引擎 KEY_LEFT/KEY_RIGHT 核对数值 | 回归注入真实按键事件验证左右移动 |

| M0-04 | Windows CI 默认终端编码无法打印中文引擎日志，Python 输出时报 UnicodeEncodeError | 验证脚本显式使用 UTF-8 标准输出与错误输出 | 在 Windows 工作流重跑 |

| M1-01 | 密集怪群互相进行角色刚性碰撞，首轮压力物理 P95 达 19.492 毫秒 | 敌人只检测世界和玩家，相互改用原生局部避让，每名代理六个近邻 | 三分钟对照结果见 docs/m1_performance.md |
| M1-02 | 只在追击状态推进导航路径，直接进入蓄力的冲锋怪没有收到避让速度，实际停在原地 | 各行动状态每物理帧都推进导航，攻击状态只覆盖期望移动速度 | 冲锋实际伤害回归通过 |
| M1-03 | 继承场景新增战斗面板位于暂停层之后，可能遮盖暂停按钮 | 暂停时隐藏战斗信息面板 | 暂停面板可见性回归通过 |
| M1-04 | 未限定根目录的 builds/ 忽略规则同时隐藏了 content/builds 装备预设 | 改为 /builds/，只忽略根目录导出产物 | 暂存文件包含两套 .tres；远端干净检出验证资源完整性 |
| M1-05 | 官方 Windows 发行模板禁用命令行场景路径覆盖，包验证使用 res:// 场景参数会退出 | 使用应用参数 --smoke-combat，让 Boot 经正常 SceneRouter 进入战斗；保留模板限制 | Windows 包独立启动验证 |

仅记录实际发现的问题。无新问题时保持此文件内容。

| M2-01 | 强类型字符串数组与未声明类型的数组拼接后，传给 Array[String] 参数触发运行时类型错误 | 校验器接收 Array，逐元素按字符串字段白名单检查 | 实际快照写入与读回通过 |
| M2-02 | JSON 将整数解析为浮点数；嵌套容器直接比较会把相同数值的不同类型判断为不一致 | 测试先统一 JSON 表示再逐字段比较；运行时显式恢复整数，64 位随机状态用字符串保存 | 场景快照对照和下一次随机结果一致 |
| M2-03 | 本轮恢复的本地引擎可执行文件只有 27 MB，运行 --version 即崩溃 | 重新下载官方同版完整压缩包并解压，确认版本后运行 | 导入与三阶段回归通过 |

| M2-04 | 输入测试复用同一个事件对象，排队后修改 pressed 导致释放事件也变成按下，背包开关被触发两次 | 测试注入事件副本并刷新输入队列 | 列表焦点下 I 关闭和 Esc 设置返回通过 |
| CI-01 | 任务文档提交 2b872a2 的首次 Windows 导出在 savepack 完成后返回 1，日志未给出具体原因；游戏与工作流文件无改动 | 对同一提交重跑失败作业，回归、导出与独立启动通过；根因尚未定位，保留退出码检查 | Actions 34492654066，第 2 次运行的对应步骤通过 |
