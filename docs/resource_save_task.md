# Resource 存档重构执行记录

基线：`30b85f01bae318ea0088d0c096e36637798ab513`。分支：`feat/resource-save`。用户已授权按冻结方案实施；旧 JSON 存档与旧探险兼容不保留。

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| S0 | 基线、分支与 Godot 4.7.2 验证环境 | 完成；原有回归通过 |
| S1 | 五模块 Resource、会话、注册及恢复协议 | 完成；包含版本升级与依赖回归 |
| S2 | 完整检查点、五手动槽、三版本自动槽、错误与索引 | 完成；已接入正式流程 |
| S3 | 业务数据接入和新角色初始化 | 完成；物品与技能回归通过 |
| S4 | Hub 保存、加载选择、自动保存、正常退出 | 完成；正式路由集成通过 |
| S5 | 旧 JSON 与旧探险移除、玩法回归迁移 | 完成；九个场景全部通过 |
| S6a | 故障恢复、真实版本升级、规模测试 | 完成；10 个常规场景和 7 个独立故障进程全部通过 |
| S6b | 架构、操作与任务文档同步 | 待执行 |
| S6c | Windows 回归、导出、交付与 PR | 待执行 |

冻结边界：仅 Hub 保存；撤离、通关、死亡保留已拾取收益并回真实 Hub。正常退出经撤离、结算、保存后完成。异常退出回到最后成功检查点。所有模块整体回退，读取不自动覆盖；不恢复战斗、地面掉落或本趟地图。独立角色组，当前仓库归角色。设置文件独立。

最新已同步代码提交：`8cdf96a13d6d388eac63f8375d8ddd393a0d8c36`；S6a 的具体提交见本记录所在提交历史。已完成五模块、正式流程、旧路径移除与故障验证；未完成文档同步、Windows 验证及 PR。

最近验证：2026-09-12，Godot `4.7.2.stable.official.ed1daf0bf`，`python3 tools/verify.py --godot <4.7.2 引擎路径>`。导入、启动、10 个常规场景和 7 个独立故障进程全部出现零失败标记。完整 166 件容量与额外 10000 件数据的真实文件往返、下一次掉落一致。10000 件一次 Linux 测量约 5.88 MB、保存 4.87 秒、读取 2.35 秒；只证明当前模型可恢复该规模，同步文本存储还不满足大仓库流畅性目标。

当前已知限制：无图形服务，槽位遮挡和布局仅完成引擎逻辑检查，实际画面与操作手感尚未验收。下一步先提交 S6a，再同步正式文档并提交，然后验证 Windows 工作流、创建 PR；每个独立可验证阶段分别提交，未经授权不合并。
