# 项目开发约束

① 开工阅读 `README.md`、`docs/engineering.md`、`docs/tasks.md` 和 `known_trap.md`；仅加载当前任务需要的文件。

② 项目使用 Godot 4.7.2 标准版、GDScript、3D 俯视即时动作、Windows 键鼠。优先使用引擎原生节点与资源。按用户决定从零开发，禁止参考其他业务仓库或做跨项目复用。

③ 从 `main` 建功能分支，`main` 只保留验收后的稳定代码。提交清晰差异及验证结果，合并按用户当前授权执行。密钥、令牌、导出凭据不得写入仓库或 `.git`。使用现有认证渠道。

④ 文件与函数采用 `snake_case`，类与节点采用 `PascalCase`，常量采用 `UPPER_SNAKE_CASE`。UTF-8、LF；GDScript 使用制表符。函数参数与返回值声明类型。

⑤ 所有新增类、函数、回调和功能写简明中文注释。使用 `## 用途`；普通实现理由用 `# 原因`。任务备注用 `# 待办[M1-编号]：具体事项与完成条件`。参数写清秒、米、速度或概率单位。

⑥ 功能场景、脚本就近存放；静态内容用 Resource 定义，装备实例单独保存抽取结果。场景内静态信号用 `.tscn` 连接；全局服务仅负责场景路由、用户设置、音频和存档。新目录随实际功能建立。

⑦ 当前任务范围见 `docs/tasks.md`。尚未完成的技能、资产和验证须如实标注。主菜单、测试场地和基础服务有实际实现，禁止用空函数冒充功能。

⑧ 使用 `python tools/verify.py --godot <引擎路径>` 验证改动。涉及渲染或操作手感还需实际试玩；无窗口测试只能证明相应逻辑。提交前查看差异，更新工作日志；实际遇到新坑才维护 `known_trap.md`。

⑨ 每完成一个独立、可验证的子任务立即提交，禁止积累到整个任务结束后统一提交。每次提交前至少完成与该子任务直接相关的最小验证，保证提交处于可运行、可继续开发的状态。

⑩ 若当前执行环境可能结束、超时或无法继续，优先停止扩展新功能，整理并提交当前最小可运行状态。中断前在当前任务卡记录：工作分支、最新提交、已完成内容、未完成内容、最近一次验证结果、当前已知问题、下一步具体操作。

⑪ 后续 Agent 接手时，以远端 Git 分支、最新提交和任务卡记录作为进度源，从中断位置继续，禁止重复实现已经完成并提交的工作。

⑫ CI、打包、外部服务或其他长耗时验证作为独立阶段执行。代码阶段完成后先提交代码，再执行长耗时验证；验证失败后的修复形成新的独立提交。

## D1～D3 任务入口

执行任务先读 `docs/demo_tasks/README.md` 与对应 `P1_TaskN.md`、`P2_TaskN.md` 或 `P3_TaskN.md`。任务基于最新 main 和已合入的前置功能，历史基线用于对照。只加载本卡需要的内容，按照本项目用户约定无需使用 `global-work-rules`。

## 完成任务前必须检查

① 查看本次差异，确认修改属于任务范围，现有未提交修改得到保留；新增类、函数、回调、功能及有单位的参数具有简明中文说明。

② 按任务卡逐项核对入口、操作与预期结果。新增资源和场景引用有效，静态定义与运行实例各负其责，当前主流程可运行。

③ 运行 `python tools/verify.py --godot <引擎路径>`，新增规则用例接入该入口并确认完成标记。记录实际版本、命令、结果；CI 执行时引用对应提交的结果，失败或未运行如实说明。

④ 涉及存档、收益、随机或物品身份时检查迁移、往返恢复、下一步行为与重复奖励；涉及视觉、声音或手感时执行对应的可见验证并保留记录。仅做文档修改时，这些玩法专项注明不适用。

⑤ 涉及发行配置或阶段验收时检查实际导出和导出包正常启动；按任务卡要求给出可用构建。无窗口运行的结论限定在其真实覆盖范围。

⑥ 更新本卡、任务总表、`docs/tasks.md` 与 `docs/work_log.md` 的实际状态；架构或操作变化同步相关文档。实际遇到新坑才更新 `known_trap.md`。

⑦ 按“改了哪些文件、当前能做什么、还不能做什么、下一步建议做什么”汇报，附分支、提交、验证与 PR。提交前确认资源、UID、文档已纳入差异，密钥和生成缓存未进入提交；合并仍按用户当前授权。
