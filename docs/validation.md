# 当前 Resource 重构验证（2026-09-12）

代码提交 `dc6ab380a7e11ef1a10039dcaaaef370876aad26`，分支 `feat/resource-save`。引擎实测 `4.7.2.stable.official.ed1daf0bf`。以下是本轮证据；后文 M0/M1/M2/P1 为历史记录，其中位置、JSON 和战斗恢复断言已经退休，不能用作当前操作说明。

```bash
python3 tools/verify.py --godot /path/to/Godot_v4.7.2
```

验证使用临时用户目录，包含编辑器导入、主菜单启动、10 个常规场景和 7 个独立故障进程。Linux 本地全部通过；对应 [Windows run 34716201490](https://github.com/w7775p/relic-ARPG/actions/runs/34716201490) 的同入口全量回归、导出与独立启动全部通过。

| 内容 | 实际覆盖与结果 |
| --- | --- |
| ResourceModel | 深拷贝隔离、必要模块、缺失内容、历史词条、新增模块默认、依赖缺失/环和未来版本拒绝；通过 |
| ResourceStore | 真实文件写入/读回、五手动槽、三版自动环、多角色隔离、缓存隔离、索引重建、路径合法性、版本一文件升级为二且原文件不变；通过 |
| SaveScale | 当前 166 件完整容量往返、所有归属、下一件掉落 ID 和词条；通过 |
| M0 / M1 | 真实输入、暂停、导航、设置，以及伤害/触发/敌人/能量；退休位置保存断言后通过 |
| M2 | 35 项：1000 件生成规则、持有归属、UI 输入、拾取过滤、三趟清场、重复奖励、撤离与死亡；通过 |
| P1Loadout / P1BleedSkills | 16 / 32 项：持久装配、技能效果/时序、暂停、收益一次性和新探险清理；通过 |
| HubFlow / DebugConsole | 22 / 6 项：真实路由、自动去抖、手动槽、加载不写档、坏档保留会话、整体回退、新角色隔离及诊断；通过 |
| 实际写入/替换失败 | 目录阻塞 pending 写入、替换瞬间真实文件系统竞争；保留当前会话和旧档/恢复副本，重试成功后离场；通过 |
| 损坏正文/缺失内容 | 实际坏 `.tres` 及未知底材，打开选择界面，失败不换当前会话，主动选择自动槽后成功；通过 |
| 索引/中断事务 | 索引替换失败正文仍成功、坏索引重建、pending 不选入、正式文件删除后可重新保存；通过 |
| 正常探险退出 | 真实探险 → Hub 结算 → 落盘成功 → 进程退出，文件金额正确；通过 |
| Windows 包 | 使用同版模板导出，独立包 headless 启动到 `M2_EXPEDITION_BOOT_READY`，退出码和日志检查通过 |
| 实际画面/字体/手感 | 未执行：当前环境无可用图形服务；逻辑布局断言不能替代可见试玩 |

故障测试只放行对应隔离路径上的已知预期 I/O 错误；脚本错误、其他引擎错误、非零退出码或缺失完成标记仍使验证失败。

Windows 包：[relic-arpg-m2-windows](https://github.com/w7775p/relic-ARPG/actions/runs/34716201490/artifacts/10304449478)，压缩产物 SHA256 `ccb21cb034526a1b13a2235bf5565591f24f9dc9a1a90edaacdc110e2db0944b`。名称沿用 M2 工作流，内容对应上述 Resource 代码提交。[独立启动日志](https://github.com/w7775p/relic-ARPG/actions/runs/34716201490/artifacts/10304289808)。

## 规模测量

同版 Linux 引擎，测试场景 `res://tests/save_scale.tscn`，166 件纳入统一入口；额外一次通过应用参数 `--scale-count=10000` 扩大合成仓库容量。测试构造真实已抽取词条和归属，写完整 `.tres`、独立读取，再核对下一件掉落。该参数仅影响测试容量，生产背包/仓库仍为 40/120。

| 总持有件数 | 文件字节数 | 保存耗时 | 加载耗时 | 测量时保留堆增量 |
| --- | --- | --- | --- | --- |
| 166 | 98440 | 77.13 ms | 30.69 ms | 1645752 字节 |
| 10000 | 5878171 | 4869.63 ms | 2349.67 ms | 95598572 字节 |

这是单次环境样本，测量早于本轮版本字段显式写入修正，正文数据模型相同，最终文件头可能相差少量字节。保留堆增量包括快照和恢复对象，不等于峰值 RSS。同步文本存档的万件性能尚不适合流畅大仓库体验，二进制/压缩、后台或分段保存须作为后续性能阶段验证；没有将其宣称为已支持大型刷宝发行规模。

## 当前 Windows 人工验收入口

① 新建两个角色，确认各自起装、背包、仓库和槽位独立；在 Hub 修改装配、锁定和仓库，等待自动保存后重启核对。分别在 1280×720、1920×1080 检查整备面板、底部按钮和槽位模态层。

② Hub 按 F5 选择空手动槽；再次保存到该槽应出现覆盖确认。制造不同金币/装配检查点，加载旧手动槽后全部模块一起回退，自动历史在读取后不新增。

③ 出发后按 F5 应提示先撤离；击杀、拾取、施放横扫/战吼/药剂，再按 T 回城保存。下一趟应无旧敌人、地面掉落或技能计时；已拾取收益和装配保留。

④ 在探险内返回菜单、退出与关闭窗口，确认先回 Hub 结算保存，重开恢复收益。与强制结束进程区分：强制结束后只恢复最后检查点。

⑤ 在独立测试档案副本中检查坏档和缺失内容提示，选择自动历史或手动槽回退；保存失败显示原因及重试，F3 可读取路径/阶段。不要用实际玩家档案制作故障样例。

## 历史阶段记录

# M0 验证记录

验证日期：2026-09-09。引擎：`4.7.2.stable.official.ed1daf0bf`。执行环境：Linux 无窗口；当前未具备 Windows 桌面或可视化渲染环境。

| 验证项 | 方法 | 结果 |
| --- | --- | --- |
| 导入与脚本加载 | `--headless --editor --import --quit` | 通过 |
| 工程入口 | 启动默认 Boot 并进入主菜单 | 通过 |
| M0 回归 | `python tools/verify.py --godot <引擎>` | 20 项断言通过，0 失败 |
| 物理与输入 | 落地、移动、闪避撞墙 | 通过 |
| 暂停 | 暂停时输入无位移，恢复后解除暂停 | 通过 |
| 存档 | 贴墙保存、写入读回、重建场景恢复、非法数据 | 通过 |
| 设置 | 设置落盘、实际音频总线音量、菜单打开 | 通过 |
| 导航 | 原生烘焙，路径绕过障碍 | 通过 |
| Windows 导出 | 同版模板 `--export-release` | 已产生 x86_64 PE 可执行文件 |
| Windows 包运行 | 工作流内独立启动，日志与退出码检查 | 工作流提交后执行；本地未验证 |
| 画面、字体、音效听感、手感 | 目标机实际试玩 | UNVERIFIED（未验证） |
| 100 / 200 怪物性能 | M1 / Demo 压力场景 | 未实现，当前没有怪物 |
| 正式模型导入与动画 | 美术源工程 → GLB → 包装场景 | 未验证，当前为原生几何体占位 |

测试进程使用临时用户目录。正式运行的用户存档不会被测试覆盖。构建日志与本地包处于工作目录，CI（持续集成）成功后提供 Windows 下载产物。

## Windows 人工验收

① 解压工作流产物并直接运行 `relic_arpg.exe`，确认中文完整、主菜单布局正常、窗口可缩放。

② 进入试炼场，移动、沿墙闪避、绕过障碍、移动鼠标检查角色朝向与镜头；验证空格冷却，确认鼠标停在菜单区域时无意外操作。

③ Esc 暂停，打开设置，调整音量并试听，切换全屏后返回。恢复时角色与镜头继续正常工作。

④ 移动到便于辨识的位置并保存，退出重开后选择继续；确认位置一致。再测试贴墙保存、保存并回菜单、保存并退出。

⑤ 在 1280×720 和 1920×1080 检查排版。记录 Windows 版本、CPU、GPU、驱动、分辨率和实际帧时间。当前结果仅用于 M0 基础体验，M1 才开始怪群性能验收。

# M1 验证记录

使用同版 Godot 4.7.2，保留 M0 的 20 项回归，新增 M1 的 26 项回归，均由实际场景执行。验证命令仍为 `python tools/verify.py --godot <引擎>`。

| 内容 | 当前结果 |
| --- | --- |
| 护甲、暴击、死亡幂等与移出目标集合 | 通过 |
| 普攻前向扇形、100 个目标旋风无漏击、墙体视线 | 通过 |
| 闪电上限与目标唯一、派生伤害不重复触发、感电状态 | 通过 |
| 已感电死亡有限传播、未感电直接死亡无爆炸、事件排空 | 通过 |
| 能量耗尽停止、闲置回复、单体命中回复、换预设保留能量 | 通过 |
| 近战、远程弹体、冲锋对玩家实际伤害 | 通过 |
| 战斗暂停、面板层级、死亡重开、清理残留弹体与事件 | 通过 |
| 持续 3 分钟目标 100 怪压力 | 数据见 m1_performance.md |
| Windows 回归、导出与包内战斗场景启动 | 最终结果见 M1 PR 对应 Actions 页面 |
| 画面、字体、音效听感、操作手感、目标机 GPU 性能 | UNVERIFIED（未验证）；当前图形显示服务不可用 |

M1 Windows 人工验收：进入场地后左键普攻、右键移动旋风，空格闪避。按 1/2 比较基础与雷霆旋风，确认新参数及说明同步；重开相同 24 怪比较效率；检查闪电可辨认、红色攻击预警清晰、死亡反馈和声音不过密。按 F6 生成 100 怪，记录目标机实际画面帧时间。按 Esc 验证暂停设置，再模拟死亡并按 R 重开。F5 只保存位置，恢复会重建敌人和基础预设。

# M2 验证记录（2026-09-10）

Godot `4.7.2.stable.official.ed1daf0bf`；Linux 无窗口。完整命令 `python tools/verify.py --godot <引擎>`。本阶段新增实际场景 `tests/m2_loop.tscn`，Windows 工作流运行相同用例，再导出并独立启动 M2 场景。

| 内容 | 实际验证 |
| --- | --- |
| 装备生成 | 固定种子抽取 1000 件，品质条数、等级限制、组互斥、数值范围和实例编号合法 |
| 实例与持有 | 独立实例、穿脱移除机制、锁定、出售定价、仓库转移、满包拾取与卸下 |
| 实际掉落与换装 | 场景击杀产出、重复死亡不重复奖励、自动金币、手动装备拾取、换装保留生命能量 |
| 界面操作 | I 开关暂停、选中物品对比、列表焦点下真实 I 按键、设置 Esc、探险拒绝出售、过滤、满包保留地面物品 |
| 完整存档 | 实际文件写入、重建场景、JSON 数字类型归一后逐字段一致、两条随机序列下一次结果一致 |
| 恢复后推进 | 弹体继续沿原方向运动并消耗寿命，冲锋蓄力恢复后执行一次攻击 |
| 损坏数据 | 重复归属、未知敌人资源、技能字段损坏被拒绝，保存失败保留旧文件 |
| 完整循环 | 连续三趟清场、对应独特奖励、升级与更高难度；提前撤离与死亡保留已拾取物品 |
| 回归结果 | M0 20 项、M1 26 项、M2 46 项；最终执行结果见本阶段 PR 对应工作流 |
| Windows 包 | 工作流检查退出码、错误日志和 `M2_EXPEDITION_BOOT_READY`；实际结果见对应提交 Actions |
| 人工视觉与手感 | 未验证；正式美术仍为占位，当前无窗口测试无法判断画面与割草手感 |

M2 人工试玩：新建角色→整备出发→击杀并拾取装备→I 查看并排词条并穿戴→继续战斗→F5 保存→退出重开继续，核对敌人、背包与掉落。清场拾取核心奖励→T 回据点→锁定、仓库转移、出售→再次出发。前三次清场后穿戴三件独特装备观察旋风闪电与爆炸；卸下一件核对机制变化。验证死亡后物品保留。在 1280×720 与 1920×1080 检查字体、滚动、标签重叠、暂停菜单和手感，再记录目标电脑帧时间。

M1 的无窗口 100 怪数据仍是历史战斗测试，不能充当 M2 带掉落标签的渲染性能结论。MVP 规则循环已有自动化证据，发布验收仍需人工试玩。

## P1_Task1 本轮验证

引擎实际版本 `4.7.2.stable.official.ed1daf0bf`。命令：`python tools/verify.py --godot /workspace/scratch/7b23297c2b83/godot-runtime/Godot_v4.7.2-stable_linux.x86_64`。编辑器导入、主入口和 M0/M1/M2/P1 完成标记全部通过，共 123 项。新增 `tests/p1_loadout.tscn` 31 项：六属性加值与取消、第四项提示、错误槽位、空槽攻击、据点权限、资源和冷却保持、上限钳制、v2 两种地点完整迁移及下一次随机、v3 非默认装配保存、重复/未知节点拒绝。原 M2 在途弹体与蓄力恢复、物品身份和奖励幂等继续通过。

图形运行：Xvfb :91 报 Cannot establish any listening sockets，未取得可见画面。人工待验收：新角色→技能与被动→选择三项与尝试第四项→出发→普攻/旋风→保存重启；1280×720 与 1920×1080 核对中文和下拉框、返回据点重置。Windows 导出与包启动结果随 PR 工作流记录。

本轮交付：功能提交 `aaf8b2e7512c491601bc1426b890cd0cb7c9c6ba`，[PR #5](https://github.com/w7775p/relic-ARPG/pull/5)。对应 [Windows 工作流](https://github.com/w7775p/relic-ARPG/actions/runs/34543081309) 的回归、导出与独立启动全部通过；[下载 Windows 构建](https://github.com/w7775p/relic-ARPG/actions/runs/34543081309/artifacts/10178021093)。产物沿用工作流名称 relic-arpg-m2-windows，实际包含本卡功能。本次后续提交仅补验证交接文档，代码与已验证提交相同。可见试玩仍待验收。

## P1_Task2 本轮验证

验证日期：2026-09-11。功能提交：`0dc446a8fdcce1d014aba6ef8c82457afa3b60c3`。GitHub Actions Windows Server 2025 环境下载并运行官方 Godot `4.7.2.stable.official.ed1daf0bf` 与同版 Windows 导出模板，命令入口为 `python tools/verify.py --godot <引擎路径>`。

| 内容 | 实际结果 |
| --- | --- |
| 全量回归 | M0 20、M1 26、M2 46、P1_Task1 31、P1_Task2 36，共 159 项，0 失败 |
| 横扫 | 前方命中、背后拒绝、3.2 米范围边缘、地形 LOS、稳定技能目录与据点装配通过 |
| 流血 | 最多三层、满层替换最短剩余层、攻击时快照、跳伤无暴击闪电及直接命中回能通过 |
| 死亡收益 | 流血击杀只登记一次击杀、经验、金币与击杀回能，死亡后续跳伤无重复收益 |
| 战吼 | 单层护甲、即时回能、重复刷新、持续结束恢复基础护甲、属性重算保留增益通过 |
| 恢复药剂 | 按最大生命比例治疗、冷却重复输入、满血、死亡和暂停拒绝通过 |
| 暂停 | 流血、战吼和药剂计时冻结，暂停期间药剂请求拒绝 |
| v4 存档 | 流血剩余/下一跳、战吼持续与冷却、药剂冷却、临时护甲完整恢复；v2/v3 迁移链通过 |
| Windows 导出 | `relic_arpg.exe` 导出成功 |
| 独立包启动 | headless 启动出现 `M1_COMBAT_BOOT_READY` 与 `M2_EXPEDITION_BOOT_READY`，日志无脚本错误 |

工作流：[run 34552955127](https://github.com/w7775p/relic-ARPG/actions/runs/34552955127)。Windows 构建：[artifact 10181489752](https://github.com/w7775p/relic-ARPG/actions/runs/34552955127/artifacts/10181489752)，工作流记录的压缩产物 SHA256 为 `d1fac666058269ea87a4bae5156b191467a3fe955461be603e4bc33b35c534df`。

当前执行容器尝试下载 Godot 4.7.2 Linux 版时无法解析 `github.com`，因此本轮没有新增 Linux 本地 Godot 结果；实际引擎证据来自上述 Windows Actions。人工待验收：可见运行横扫方向反馈、流血辨识度、战吼和药剂反馈、中文排版、声音听感与操作手感。交付 PR：[PR #6](https://github.com/w7775p/relic-ARPG/pull/6)。

## P1_Task2 Windows 保存失败诊断（2026-09-11）

用户在 Windows 发布包人工验收时点击保存实际显示“保存失败”。CI 的 v4 存档写入此前一直成功，因此无法从旧提示判断用户机器失败发生在结构校验、临时文件写入、flush 或正式文件替换中的哪一步。本轮增加游戏内 F3 DebugLog，读取 `user://logs/godot.log`；SceneRouter 启动时打印 Godot 版本、系统、`user://` 和 `session.json` 绝对路径，SaveManager 打印保存各阶段与错误码。

| 内容 | 实际结果 |
| --- | --- |
| 原 M0/M1/M2/P1 回归 | 159 项继续通过 |
| DebugLog 新回归 | 6 项通过：常驻控制台、F3 语义动作、发布包 stdout 实时 flush、固定日志路径、运行中读到探针、覆盖层打开 |
| 合计 | 165 项，0 失败 |
| Windows 存档写入 | CI 临时 Windows 用户目录多次创建/覆盖 `session.json` 成功；日志记录绝对路径与写入字符数 |
| 首次发布包验证 | 失败：运行期控制台位于 `debug/`，被 `exclude_filter="tests/*,debug/*"` 排除；独立启动捕获 preload 缺失 |
| 修正 | 将运行期控制台迁到 `ui/debug/`，开发用 `debug/*` 继续排除 |
| 修正后发布包 | run 34557223053：导入回归、Windows 导出、独立启动、包上传全部通过 |

修正工作流：[run 34557223053](https://github.com/w7775p/relic-ARPG/actions/runs/34557223053)。Windows 构建：[artifact 10183010207](https://github.com/w7775p/relic-ARPG/actions/runs/34557223053/artifacts/10183010207)，SHA256 `e84b77a7188f6e79feb6beeff40fc58660501bebbcd22845048325a71d3d4cd4`。下一项人工验证：使用该新包点击保存；若仍失败，F3 打开控制台并记录最新 `[DEBUGLOG][ERROR]` 及其前后保存步骤，据此定位用户机器上的真实失败原因。
